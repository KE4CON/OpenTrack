@page "/issues"
@attribute [Authorize]
@inject IOpenTrackDataService Data

<PageTitle>Issues</PageTitle>

<h1>All Issues</h1>

<div class="mb-3 col-md-4">
    <label class="form-label">Filter by project</label>
    <select class="form-select" @bind="selectedProjectId">
        <option value="0">All Projects</option>
        @foreach (var p in projectOptions)
        {
            <option value="@p.Id">@p.Name</option>
        }
    </select>
</div>

@if (FilteredIssues.Count == 0)
{
    <p class="text-muted">No issues match this filter.</p>
}
else
{
    <table class="table table-hover align-middle">
        <thead>
            <tr>
                <th>#</th>
                <th>Project</th>
                <th>Title</th>
                <th>Status</th>
                <th>Priority</th>
                <th>Reporter</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var i in FilteredIssues)
            {
                <tr>
                    <td>@i.Id</td>
                    <td>@i.ProjectName</td>
                    <td><a href="issues/@i.Id">@i.Title</a></td>
                    <td>@i.Status</td>
                    <td>@i.Priority</td>
                    <td>@i.ReporterName</td>
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private IReadOnlyList<IssueRow> allIssues = [];
    private List<(int Id, string Name)> projectOptions = [];
    private int selectedProjectId;

    private IReadOnlyList<IssueRow> FilteredIssues =>
        selectedProjectId == 0 ? allIssues : allIssues.Where(i => i.ProjectId == selectedProjectId).ToList();

    protected override async Task OnInitializedAsync()
    {
        projectOptions = (await Data.GetProjectsAsync())
            .Select(p => (p.Id, p.Name))
            .ToList();

        allIssues = await Data.GetIssuesAsync();
    }
}
